<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Shop{% endblock %}</title>

        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
        {% endblock %}
    </head>
    <body>
	{% block header %}
	    <nav class="navbar">
		<a href="{{ path('home') }}" class="navbar-home">
		    Shop
		</a>
		<a href="{{ path('add') }}" class="navbar-home">
		    Add product
		</a>  
		<a href="{{ path('cart') }}" class="navbar-cart">
		    Cart
		</a>
	    </nav>
	{% endblock %}
        {% block body %}{% endblock %}
        {% block javascripts %}
        {% endblock %}
    </body>
</html>
